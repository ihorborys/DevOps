apiVersion: v2
name: django-app
description: A Helm chart for my Django application on AWS EKS
type: application
version: 0.1.0
appVersion: "1.0.0"

# ДОДАЄМО ЦЕЙ БЛОК:
dependencies:
  - name: postgresql
    version: 12.1.0
    repository: https://charts.bitnami.com/bitnami